<Page
    x:Class="MVVMApp1.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MVVMApp1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top">
            <TextBlock TextWrapping="Wrap" Text="{x:Bind ViewModel.Age, Mode=OneWay}" />
            <TextBox TextWrapping="Wrap" Text="{x:Bind ViewModel.Age, Mode=TwoWay}" />
            <TextBlock TextWrapping="Wrap" Text="{x:Bind ViewModel.FirsName, Mode=OneWay}" />
            <TextBox TextWrapping="Wrap" Text="{x:Bind ViewModel.FirsName, Mode=TwoWay}" />
        </StackPanel>
    </Grid>
</Page>
